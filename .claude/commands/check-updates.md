# Check for Updates

Check if a newer version of Guardian-Agent is available.

Use the Guardian-Agent `check_for_updates` tool to:
1. Check the current installed version
2. Fetch the latest version from GitHub
3. Show changelog if update is available
4. Provide instructions for updating

If an update is available, ask if the user wants to update now.
